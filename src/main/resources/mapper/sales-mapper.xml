<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 해당 파일에 모든 쿼리문을 작성 -->
<!-- namespace에는 xml파일과 연결할 Mapper interface 파일명을 패키지를 포함하여 적어주세요 -->
<mapper namespace="com.green.backend_car.sales.mapper.SalesMapper">
    <resultMap id="salesList" type="com.green.backend_car.sales.dto.SalesDTO">
        <id column="SALE_NUM" property="saleNum" />
        <result column="BUYER_NAME" property="buyerName" />
        <result column="BUYER_CONTACT" property="buyerContact" />
        <result column="COLOR" property="color" />
        <result column="SALE_DATE" property="saleDate" />
        <result column="MODEL_NUM" property="modelNum" />
        <result column="MODEL_NAME" property="modelName" />
        <result column="PRICE" property="price" />
    </resultMap>

    <insert id="insertSales">
        INSERT INTO SALES_INFO (BUYER_NAME
            , BUYER_CONTACT
            , COLOR
            , SALE_DATE
            , MODEL_NUM)
        VALUES (#{buyerName}
            , #{buyerContact}
            , #{color}
            , #{saleDate}
            , #{modelNum})
    </insert>

    <select id="getSalesList" resultMap="salesList">
        SELECT S.SALE_NUM
                , S.BUYER_NAME
                , S.BUYER_CONTACT
                , S.COLOR
                , S.SALE_DATE
                , S.MODEL_NUM
                , C.MODEL_NAME
                , C.PRICE
        FROM
        SALES_INFO JOIN CAR_INFO
        ON S.MODEL_NUM = C.MODEL_NUM
        ORDER BY
        S.SALE_NUM DESC
    </select>
</mapper>

